<?php    exit('success');   // /**   //  *通知方法  cp 自行编写   //  */   //  public function notify($signdata)   //  {           // ksort($signdata);           // foreach ($signdata as $k => $v) {           //     if(!empty($v)){           //         $tmp[] = $k . '=' . $v;           //     }           // }           // $str = implode('&', $tmp) . KEY;           // $sign = md5($str);           // if($sign!=$signsure){           //     \Think\Log::record("sign验证失败");           //     $this->ajaxReturn(array('code'=>1002,'msg'=>'sign验证失败'));           // }                      // if(empty($signdata['trade_no'])){           //     $channepay->error_record("透传信息无效");           //     $this->ajaxReturn(array('code'=>1019,'msg'=>'透传信息无效'));           // }           // $pay_map['pay_order_number'] = $signdata['trade_no'];           // $param = M("Spend","tab_")->where($pay_map)->find();           // if(empty($param)){           //     $channepay->error_record("订单不存在");            //     $this->ajaxReturn(array('code'=>1020,'msg'=>'订单不存在'));           // }elseif(round($param['pay_amount'],2)!=round($_REQUEST['amount']/100,2)){           //     $channepay->error_record("订单金额不一致");           //     $this->ajaxReturn(array('code'=>1020,'msg'=>'订单金额不一致'));           // }elseif($param['pay_status'] == 1 && $param['pay_game_status'] == 1){           //     $channepay->error_record("订单重复");            //     $this->ajaxReturn(array('status'=>'success'));           // }   //  }   // }